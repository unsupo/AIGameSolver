# Scripts Directory 🛠️

Utility scripts and native bridge components.

- `gba_bridge.so`: The compiled C bridge for mGBA.
- `cleanup_memory.py`: A script to prune and optimize the long-term memory database.
- `auto_bootstrap.py`: Initial setup and dependency verification.
